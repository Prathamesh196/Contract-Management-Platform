# Contract-Management-Platform
Contract Management Platform built with React and TypeScript. Create reusable contract blueprints, generate contracts, manage them through a strict lifecycle (Created to Locked/Revoked), and track all contracts in a dashboard. Frontend-only with localStorage persistence.
